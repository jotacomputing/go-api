package main

import (
	"fmt"
	"log"
	"math/rand"
	"os"
	"time"
)

const queueFilePath = "/tmp/sex"
const order = queue.Order{
	order_id:   3, //uint64
	price: 		12000, //uint64
	timestamp: 	uint64(time.Now().UnixNano()),
	user_id:  	1001,
	shares_qty: 16,
	symbol:    	0,
	side: 		0,
	order_type: 0,
	status:    	0, // pending
}
	
func main() {
}
